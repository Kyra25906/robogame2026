# 工程开发纪律（RoboGame 2026）

> 目的：这个项目不只是「完成学校比赛」，而是达到「可以写进简历、经得起面试追问、半年后回看仍然干净」的工程标准。本纪律是日常开发的底线，每一条都对应一项可写入简历的证据。

## 总纲

**用证据说话，而不是用形容词说话。**

简历上能写「258 项测试 0 失败」，是因为仓库里有一条条可复现的测试；能写「真车闭环 10 次成功 9 次」，是因为有一张张当场记录的验收表。**任何一条简历条目，都必须能在仓库里找到对应的证据文件。** 找不到证据的内容，宁可不上简历。

## 八条纪律

### 1. 证据分级：写「能证明什么」，不写「我完成了什么」

- **做法**：每次收工，结论拆成三栏——已验证 / 未验证 / 不能证明。
- **为什么**：模拟通过、构建通过、固定样本通过，都只能证明有限的东西。把「没观察到失败」写成「永不失败」，是简历上最容易被打假的坑。
- **简历证据**：面试能答「你的代码证明了什么、没证明什么」——这是初级工程师最稀缺的品质。

### 2. 可复现优先于「在我机器上能跑」

- **做法**：任何功能交付都附带：入口命令、输入、预期输出、验证方式。`GETTING_STARTED` 必须能让一个从没接触过项目的陌生人在干净环境跑通。
- **为什么**：面试官会点开 GitHub。他跑不起来，你的简历就只剩一堆文字。
- **简历证据**：README 顶部一段 30 秒、陌生人可复现的演示。

### 3. 改核心逻辑，先写测试；改完跑回归

- **做法**：核心逻辑（导航、状态机、协议编解码）改动前先补测试，改完依次跑：针对性测试 → 所属模块测试 → 相关回归测试。
- **为什么**：没有测试兜底的重构，等于拿整辆车赌运气。
- **简历证据**：`tests/` 目录 + 一个稳定增长、且你讲得清每项测了什么的测试数量。

### 4. 一次提交只做一件事，提交信息说清「为什么」

- **做法**：一个 commit 一个意图，禁止「改了一堆 + 顺手修了别的」。提交信息写清：改了什么、为什么、验证了什么。
- **为什么**：Git 历史本身就是作品集，面试官 `git log` 一眼能看出你的工程习惯。
- **简历证据**：干净、可读、能讲出「每个 commit 在解决什么」的分支与提交历史。

### 5. 证据当场采集，事后无法补

- **做法**：真车联调时，按验收清单边跑边抓——视频、成功率表、失败现象、定位误差、日志，当场回填到 `results/` 或对应的 field 文档。
- **为什么**：真车的视频和数据无法事后补拍。等车拆了、场地没了，「10 次成功 9 次」就只是一个没人信的说法。
- **简历证据**：README 顶图视频 + 一张可量化的验收表（成功率、耗时、误差）。

### 6. 不为了「看起来厉害」加复杂度

- **做法**：每引入一个抽象、一个依赖、一个框架，都必须能回答「它解决哪个真实问题」。答不上来就不加。P 控制够用就不上 nav2，手写校验器够用就不引 pydantic。
- **为什么**：过度设计会拖慢进度、制造 bug，还会在面试被追问「你为什么用这么重的方案」时露馅。
- **简历证据**：能说清「为什么选这个方案、为什么没选更重的方案」——这比「我用了最流行的技术」值钱。

### 7. 对真实系统的新认知，必须写进 fake，不能只写进文档

- **做法**：每次搞清楚真实硬件/固件的一个行为——尤其是它**会拒绝什么**——同一次提交里就把它加进假固件与契约测试，而不是只记进 `TODO_AND_ISSUES.md`。假固件的职责是**忠实模仿真实系统的严格程度**，不是"让测试容易过"。
- **为什么**：**文档不会让 CI 变红，fake 会。** 2026-08-17 第三轮复审的六个 P0 全部由人工走读源码发现，344 个测试无一捕获——因为 30 个测试文件里只有 3 个提到 field/real 模式，且全是配置元数据测试；而硬件边界 `robot_bridge/node.py` 上有 23 处 mock/real 分叉，真实那一侧的行为测试为零。mock 宽容、真实固件严格，测试只走宽容侧，真实侧每条拒绝路径都是盲区。代价最大的一次：ISSUE-021（限幅全 `0.0f`，固件拒绝一切自动速度命令 = 车根本不能自主移动）写进了文档，却没写进 fake，直到几周后读固件源码才发现。
- **推论——分清两类问题**：必须上车的是**「量」与物理现象**（焦距、HSV 阈值、航点坐标、解算补偿系数、CPU 余量、打滑、遮挡、机构重复精度）；**「逻辑」缺陷一律不得留到现场**（状态机误判死、配置漏传、真实分支不支持某命令、枚举漏成员、订阅话题无发布者）。逻辑缺陷离线发现花几分钟，上车发现要烧掉一整个场地时段——而场地时段是最稀缺资源。
- **简历证据**：一个能说清"我们的假固件会拒绝什么、为什么这样设计"的测试套件，以及"发现→写进 fake→CI 变红→修复"的提交链。这比"我写了 344 个测试"值钱得多。
- **推广——知识必须沉到最深那一层**：本项目的缺陷几乎都能归到"知识停留在太浅的层级"：

  | 层级 | 载体 | 谁会强制执行 | 本项目实例 |
  |---|---|---|---|
  | 三（最深） | fake / 测试 / 断言 | **CI，无条件** | 规则 3.2.1 载货约束（`Cargo.can_add` 总数 ≤3、紫色 ≤1）——唯一沉到底的一条 |
  | 二 | 文档 | 人，如果他记得读 | ISSUE-021 限幅全 `0.0f`、RETREAT 责任归属——写了，没人执行 |
  | 一（最浅） | 只在对话里说过 | **没有人** | 斜坡与高台的存在。git 取证：2026-08-03 建仓到 08-17，**十四天内「斜坡」二字未出现在任何提交**；「高台」首次出现在 08-18。场地图 3.1/3.8/3.9/3.10 在 08-18 之前**始终未入库** |

  **停在第一层的知识，下一个 session 就等于不存在。** 每次讨论出结论，必须立刻问：这条能沉到第三层吗？不能的话至少沉到第二层，且在执行队列里有编号。

### 8. 「依赖硬件」必须拆成「开发依赖」和「验收依赖」——判据是能不能 fake

- **做法**：任何时候要写下或说出「这个依赖硬件 / 要等硬件」，**必须**先拆成两栏：
  - **验收依赖**：没有真硬件就无法*证明*它对（物理量、物理现象）。
  - **开发依赖**：没有真硬件就无法*开始写*。
  **判据只有一条：能不能写出忠实 fake。** 能 fake → 不是开发阻塞项，今天就能写代码 + 单测。fake 不出来 → 才是真阻塞。
- **示例**：AprilTag 检测（合成图可 fake）→ 不阻塞。坡道 pitch 补偿（合成 IMU 序列可 fake）→ 不阻塞。巡线控制律（合成传感器序列可 fake）→ 不阻塞。焦距实测值 / 现场 HSV 阈值 / 航点坐标 / 红外电位器阈值（**就是物理量本身，fake 不出来**）→ 真阻塞，但只阻塞验收，不阻塞代码。STM32 限幅从 `0.0f` 改为非零（只有真固件能驱动电机）→ 真阻塞。
- **为什么**：`docs/` 全目录中「开发依赖」「验收依赖」二词出现 **0 次**，而「依赖 / 等待 / 现场」在 `TODO_AND_ISSUES.md` 出现 45 次。一个「依赖」同时承担两种含义，结果是整层 P1 工作被误判为「要等硬件」而搁置数周。反例就在同一份文档里：`第三轮复审修复方案_2026-08-17.md` 第 195 行标题明确写着「不阻塞软件修复，但决定验收」，第 14 行讲 P0 时也写了「相机驱动安装是现场依赖，**但 launch 配置可以先写**」——**这个区分只在讲 P0 处存在，讲 P1 处（第 144/154/212/213 行）全部退化成裸的「依赖相机」「依赖 IMU」**，而那正是 agent 决定下一步时最先读的段落。
- **另一层原因（须自觉对抗）**：「这个要等硬件」是一句**今天无法被证伪**的话；「用合成图今天就能写完检测器并跑单测」是一句**可核查的断言**。在「安全但没用」和「有用但可能错」之间，默认会滑向前者。**可核查性是硬要求，不是加分项。** 不得让使用者靠反复追问来人工纠正这个偏置。
- **与第 7 条的关系**：第 7 条说「把你对真实硬件的认知写进 fake」；本条是它的逆命题——**「如果你能 fake 它，你就没被卡住」**。同一原理的两个方向。
- **简历证据**：一张把每个待办明确标注「开发可离线 / 验收需硬件」的执行队列，以及在硬件到位前就已完成并通过单测的算法模块。

## 完成标准（Definition of Done）

一个功能算「完成」，必须同时满足：

1. 有入口命令，陌生人可复现；
2. 有对应测试，且通过；
3. 结论按「已验证 / 未验证」分级写清；
4. 提交信息说清「为什么」；
5. 若涉及真车或硬件，证据当场采集并存档。

缺任何一条，就只能叫「写了一半」，不能叫「完成」，更不能写进简历。

## 与 AGENT.md 的关系

- `AGENT.md` 管「怎么和 Agent 协作、每轮怎么收工」；
- 本纪律管「工程性底线，能不能写进简历」。
- 两者冲突时，以「证据分级」和「不过度设计」为准。
