# RoboGame2026 2026-08-13 至 08-18 执行计划（v2 快进版）

适用阶段：由「远程软件准备 + 通信学习完成」转入「真车接入 + 单方块物理闭环」。
本计划取代 `PLAN_2026-08-09_TO_14.md`。原则不变：安全、单点、闭环、重复；阶段不跳过，但把不依赖协议的软件工作尽量前移，为尽早完成（目标 15/16 号）压缩时间。

## 总目标

尽早（力争 15/16，最晚 18）完成一个可重复、有完整证据的单方块物理闭环：

```text
软件安全与功能缺口清零（不依赖协议，13—14 号）
→ 电控/机械协议冻结（外部依赖，不可控）
→ 协议离线实现 + 固定样例测试
→ 真车只读通信
→ STOP / 急停 / 失联停车
→ 低速底盘 → 机构单动作 → 单方块闭环
→ 重复与统计
```

## 两个并行轨道

### 轨道 A：软件（不依赖协议，13—14 号清完）

- [ ] `ISSUE-015` 串口读写异常保护（`robot_bridge/node.py` 132/214/302 行）
- [ ] `ISSUE-016` 命令超时向 MCU 发零速度帧（`node.py` 319-320 行）
- [ ] `ISSUE-017` 机构取消命令：cancel 路径显式调 `/chassis/stop`
- [ ] `SEARCH` 旋转搜索 + 目标角度 `wz` 控制（mock 可验证）
- [ ] 双证据 / 掉块 / 放置稳定验证的策略骨架（先 `service_only` 兼容，真传感器到位后切换）

### 轨道 B：硬件（依赖电控/机械交付协议，交付后依次推进）

```text
协议冻结 → 协议离线实现 → 只读通信 → 安全门 → 单动作 → 闭环 → 重复
```

轨道 B 的日期依赖电控（0x10/0x11/0x12 逐字节表 + 真实十六进制样例）与机械（动作完成判据 + STOP/CANCEL）的交付时间，我方不压缩安全门。

## 每日安排

### 8 月 13 日（今天）：缺口登记 + 串口安全开工

- [x] 记录软件缺口到 TODO（本次）
- [ ] 修 `ISSUE-015` / `ISSUE-016` 并跑回归（用 mock serial 验证代码路径，不依赖真 MCU）
- [ ] 再次向电控索要 `0x10/0x11/0x12` 逐字节表 + 真实十六进制样例；向机械索要动作完成判据 + STOP/CANCEL 行为

### 8 月 14 日：软件缺口清零

- [ ] 完成 015/016 回归；实现 cancel 调 STOP + SEARCH/wz 骨架
- [ ] 复跑 Windows 全量测试 + Ubuntu 干净构建（留证据）
- [ ] 若协议已到 → 立即转阶段 4：把真实十六进制样例写成固定输入解码测试

### 8 月 15 日（目标日 A）：协议离线实现 + 只读通信准备

- [ ] 阶段 4：ODOM/IMU/STATUS 解码适配器 + 固定样例测试（不直接上真车试错）
- [ ] 阶段 5：STM32 只读连接，观察帧/状态/新鲜度/重启/断线恢复（执行器无动力）
- [ ] 若协议仍未到 → 退而采集/整理视觉数据，不跳过安全门

### 8 月 16 日（目标日 B）：安全门 + 单方块闭环

- [ ] 依次通过：上电不动 → STOP → 急停 → 失联停车 → 低速底盘单轴 → 机构空载/带载
- [ ] 第一次单方块 PICK/PLACE 闭环（有人监护、随时可急停）
- [ ] 失败按层归类：视觉 / 定位 / 运动 / 通信 / 机构 / 证据 / 超时

### 8 月 17 日：重复性与统计

- [ ] 相同条件重复闭环，统计成功率、平均耗时、失败原因
- [ ] 复验急停 / 失联 / 超时 / 取消 / 重启恢复

### 8 月 18 日（缓冲日）：备份与交接

- [ ] 备份视频 / CSV / JSONL / HTML / 日志 / 参数快照
- [ ] 更新 TODO、学习记录、现场交接；Git 检查点

## 不可违反的规则

1. 未确认的 MCU/机构载荷不得靠猜测写入代码。
2. 急停、失联停车、STOP 未通过前，不运行自动导航和机构动作。
3. 每次只引入一个真实变量。
4. 现场参数修改记录前值、原因、条件、结果和数据路径。
5. 8 月 13 日起停止非必要重构，以回归、稳定性和恢复为主。
6. 15/16 目标日的前提是电控/机械交付协议；未交付则只推进轨道 A + 视觉数据，不压缩安全门。

## 每日收尾模板

```text
今天的唯一目标：
完成证据：
没有完成的原因：
新增问题与风险：
修改的参数和代码：
数据保存路径：
明天第一步：
```
