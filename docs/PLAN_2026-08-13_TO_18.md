# RoboGame2026 2026-08-13 至 08-18 执行计划（v2 快进版）

适用阶段：由「远程软件准备 + 通信学习 + 视觉现场标定完成」转入「真车接入 + 单方块物理闭环」。
本计划取代 `PLAN_2026-08-09_TO_14.md`。原则不变：安全、单点、闭环、重复；阶段不跳过，但把不依赖外部交付的工作尽量前移，为尽早完成（目标 15/16 号）压缩时间。

## 总目标

尽早（力争 15/16，最晚 18）完成一个可重复、有完整证据的单方块物理闭环：

```text
软件安全修复（已完成）
+ 视觉检测/标定（已完成）+ 视觉→集成接口（SEARCH/ACQUIRE/VERIFY、目标选择、distance_valid）
+ 电控/机械协议冻结（外部依赖，不可控）
→ 协议离线实现 + 固定样例测试
→ 真车只读通信
→ STOP / 急停 / 失联停车
→ 低速底盘 → 机构单动作 → 视觉证据 → 单方块闭环
→ 重复与统计
```

## 三条并行工作流

### 轨道 A：软件安全（不依赖协议，已完成 2026-08-13）

- [x] `ISSUE-015` 串口读写异常保护（`bc4b920`）
- [x] `ISSUE-016` 命令超时向 MCU 发零速度帧（`9717cb9`）
- [x] `ISSUE-017` cancel 显式调 `/chassis/stop`（`aaadd40`）

### 轨道 B：视觉（检测/标定已完成，剩接口与现场项）

已完成（`fd568f0` 合并进主线）：触边拒绝、短边测距、`max_working_distance_m=1.2`、GF100 标定（RMS 0.82px）、距离验证（60 样本）、角度/遮挡分析。

待办：

- [ ] `P0` 视觉阶段 SEARCH / ACQUIRE / VERIFY：集成驱动视觉节点进入不同阶段（远距搜索 / 近距抓取 / 抓放验证），解锁「找目标 → 抓取」。
- [ ] `P0` 多合法同色目标选择约束：材料区多目标时集成提供 `desired_color`/槽位/中心等约束，不能只靠最高置信度猜。
- [ ] `P0` `distance_valid` 字段：表达「检测到但距离因侧转/遮挡不可信」，避免整候选拒绝。
- [ ] `P1` P1-3 车载 ROI 与自遮挡区：等机械结构冻结。
- [ ] `P1` P1-4 0.3m/0.4m 近距：等工作距离确认。

### 轨道 C：硬件（依赖电控/机械交付，交付后依次推进）

```text
协议冻结 → 协议离线实现 → 只读通信 → 安全门 → 单动作 → 视觉证据 → 闭环 → 重复
```

轨道 C 日期依赖电控（0x10/0x11/0x12 逐字节表 + 真实十六进制样例）与机械（动作完成判据 + STOP/CANCEL + 相机安装位）交付，我方不压缩安全门。

## 每日安排

### 8 月 13 日（今天，已完成）：缺口登记 + 串口安全 + 视觉进度同步

- [x] 记录软件缺口到 TODO
- [x] 修 `ISSUE-015` / `ISSUE-016` / `ISSUE-017`，Windows 全量 258 项通过
- [x] 核实视觉已合并（`fd568f0`），把标定/距离/角度/遮挡进展补进 TODO
- [ ] 再次向电控索要逐字节表 + 真实样例；向机械索要动作判据 + STOP/CANCEL + 相机安装位

### 8 月 14 日：视觉→集成接口设计 + 剩余软件收尾

- [ ] 审计 `cube_perception/node.py` 现状，设计 SEARCH/ACQUIRE/VERIFY 阶段接口（最小改动）
- [ ] 对齐多目标选择约束 + `distance_valid` 字段的消息接口方案
- [ ] 若电控/机械交付 → 转阶段 4 协议离线实现；否则继续视觉接口实现
- [ ] 复跑 Windows 全量 + Ubuntu 干净构建（留证据）

### 8 月 15 日（目标日 A）：协议离线实现 + 只读通信准备

- [ ] 阶段 4：ODOM/IMU/STATUS 解码适配器 + 固定样例测试
- [ ] 阶段 5：STM32 只读连接（执行器无动力）
- [ ] 视觉接口若已完成，离线跑 SEARCH→ACQUIRE→VERIFY 阶段的 mock 闭环

### 8 月 16 日（目标日 B）：安全门 + 单方块闭环

- [ ] 依次通过安全门：上电不动 → STOP → 急停 → 失联停车 → 低速底盘 → 机构空载/带载
- [ ] 相机固定安装 + 车载 ROI 确认（P1-3）
- [ ] 第一次单方块 PICK/PLACE 闭环（有人监护）
- [ ] 失败按层归类：视觉 / 定位 / 运动 / 通信 / 机构 / 证据 / 超时

### 8 月 17 日：重复性与统计

- [ ] 相同条件重复闭环，统计成功率、耗时、失败原因
- [ ] 复验急停 / 失联 / 超时 / 取消 / 重启恢复

### 8 月 18 日（缓冲日）：备份与交接

- [ ] 备份视频 / CSV / JSONL / HTML / 日志 / 参数快照
- [ ] 更新 TODO、学习记录、现场交接；Git 检查点

## 不可违反的规则

1. 未确认的 MCU/机构载荷不得靠猜测写入代码。
2. 急停、失联停车、STOP 未通过前，不运行自动导航和机构动作。
3. 每次只引入一个真实变量。
4. 现场参数修改记录前值、原因、条件、结果和数据路径。
5. 8 月 13 日起停止非必要重构，以回归、稳定性和恢复为主。
6. 15/16 目标日的前提是电控/机械交付协议 + 相机安装位；未交付则只推进轨道 A/B（软件 + 视觉接口），不压缩安全门。

## 每日收尾模板

```text
今天的唯一目标：
完成证据：
没有完成的原因：
新增问题与风险：
修改的参数和代码：
数据保存路径：
明天第一步：
```
