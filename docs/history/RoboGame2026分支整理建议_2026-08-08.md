# RoboGame2026 Git 分支整理建议

生成日期：2026-08-08  
审计基准：远端 `origin` 刷新后的分支状态  
当前开发分支：`codex/hardware-readiness`  
当前提交：`304623d`

> 本文用于帮助团队判断哪些分支需要合并、保留或删除。删除分支前，应先确认对应成果已经进入 `main` 或其他保留分支。

## 一页结论

| 分支 | 当前情况 | 是否需要合并到 main | 现在能否删除 | 建议动作 |
|---|---|---:|---:|---|
| `main` | 稳定主线；包含两份 GitHub 上传的比赛资料 | 不适用 | 否 | 保留，避免直接在上面开发 |
| `codex/hardware-readiness` | 当前主要开发成果；比 main 多 41 个提交、少 1 个资料提交 | **需要** | 否 | 创建 PR，评审并验收后合入 main |
| `codex/manipulator-mock-closed-loop` | 3 个提交已被 hardware-readiness 完整继承 | 不需要单独合并 | 暂缓 | hardware-readiness 合入 main 后删除 |
| `codex/vision-batch-annotation` | 14 个提交已被 hardware-readiness 完整继承 | 不需要单独合并 | 暂缓 | hardware-readiness 合入 main 后删除 |
| `feature/localization` | 5 个独立提交尚未进入当前分支；部分功能与当前代码重叠 | **以后单独处理** | 否 | 保留；基于新 main 解决冲突后另提 PR |
| `feature/vision-integration` | 已合入 main | 不需要 | **可以** | 可删除远端和本地旧分支 |
| `feature/vision-report` | 已合入 main | 不需要 | **可以** | 可删除远端和本地旧分支 |

## 分支关系

```text
main
│
├─ feature/vision-integration       已合并 main，可删除
├─ feature/vision-report            已合并 main，可删除
│
├─ codex/manipulator-mock-closed-loop
│      ↓ 已被后续分支完整继承
├─ codex/vision-batch-annotation
│      ↓ 已被后续分支完整继承
└─ codex/hardware-readiness         当前主要合并候选

feature/localization                队友独立工作，尚未合并
```

## 为什么只合并 hardware-readiness

`codex/hardware-readiness` 已经按提交历史继承了：

1. `codex/manipulator-mock-closed-loop` 的全部 3 个提交；
2. `codex/vision-batch-annotation` 的全部 14 个提交；
3. 后续硬件安全、配置检查、协议测试、Ubuntu 验收及文档整理。

因此不要把三个 `codex` 分支分别合入 `main`。只评审最前面的 `codex/hardware-readiness`，即可一次包含前面的成果。

## main 多出来的一个提交

`origin/main` 比当前开发分支多：

```text
8fd9bfc Add files via upload
```

该提交只增加两份比赛资料：

- RoboGame2026 竞技组规则手册 PDF；
- 二审评分细则 Word 文档。

它不是代码功能。通过 PR 把 `codex/hardware-readiness` 合入 `main` 时，应确认这两份资料仍然存在。

## hardware-readiness 合并前检查

- [ ] PR 方向正确：`codex/hardware-readiness → main`。
- [ ] GitHub 没有意外删除比赛规则 PDF 和评分细则 Word。
- [ ] PR 文件列表主要符合当前开发内容，没有本地视频和 `results/`。
- [ ] Windows 全项目测试仍为 185 项通过。
- [ ] Ubuntu 能构建 9 个 ROS2 包。
- [ ] 配置检查输出 `CONFIG PASS: errors=0 warnings=0`。
- [ ] mock 闭环和 field 无硬件失败安全 smoke 有通过记录。
- [ ] 至少一位队友阅读 PR 中的接口、安全和配置变化。

## localization 为什么不能直接合并

`feature/localization` 包含：

- `quality.py` 传感器有效性辅助逻辑；
- IMU 过期时降级；
- 定位测试和说明；
- `navigation_acceptance.py` 导航验收工具。

但当前 `codex/hardware-readiness` 已经实现了基于 `RobotStatus.imu_valid` 和状态新鲜度的降级，两边会同时修改：

- `localization/node.py`；
- `robot.yaml`；
- IMU 有效性判断。

直接合并可能覆盖当前安全逻辑。建议顺序：

```text
hardware-readiness 合入 main
→ 队友把最新 main 同步到 feature/localization
→ 人工解决 node.py 和 robot.yaml 冲突
→ 保留队友的质量测试和导航验收工具
→ 运行 Windows 测试与 Ubuntu 构建
→ localization 单独创建 PR
```

## 推荐整理顺序

### 阶段一：合并当前主要成果

1. 创建 `codex/hardware-readiness → main` PR；
2. 检查资料文件、代码变化和自动测试；
3. 合并 PR；
4. 基于合并后的 `main` 再做一次 Ubuntu 干净验收。

### 阶段二：删除已经没有独立价值的分支

确认第一阶段成功后，可删除：

```text
feature/vision-integration
feature/vision-report
codex/manipulator-mock-closed-loop
codex/vision-batch-annotation
```

### 阶段三：处理定位分支

保留并整理：

```text
feature/localization
```

待队友将其更新到新 `main`、解决冲突并完成验收后，再通过单独 PR 合并。

### 阶段四：建立现场开发分支

完成上述合并后，可从最新 `main` 创建：

```text
codex/field-integration
```

现场新增代码只进入这个分支，不再继续叠加旧的远程阶段分支。

## 理想的最终分支结构

现场开发开始时，远端最好只保留：

```text
main                       已验收的稳定主线
feature/localization       队友尚未合入的定位工作
codex/field-integration    现场新增工作
```

定位分支合并完成后，进一步简化为：

```text
main
codex/field-integration
```

## 新手需要理解的三个概念

### 已合并不等于分支必须继续保留

分支只是指向某个提交的名字。代码进入 `main` 后，删除旧分支不会删除已经合并的代码。

### 被后续分支继承不等于已经进入 main

旧 `codex` 分支的内容已经进入 `hardware-readiness`，但尚未进入 `main`。因此应等最前面的分支合入 `main` 后，再删除这些旧分支。

### 领先和落后只是提交关系

“领先 41、落后 1”表示：

- 当前分支有 41 个 main 没有的提交；
- main 有 1 个当前分支没有的提交。

它不直接表示代码质量高低，也不表示一定有冲突。

## 操作红线

- 不在未确认继承关系前删除分支；
- 不把多个有继承关系的旧分支重复合并；
- 不在 `main` 上直接继续现场开发；
- 不使用 `git reset --hard` 清理分支；
- 不因分支名看起来旧就判断它已经无用；
- 删除远端分支前，先确认对应 PR 和 Ubuntu 验收结果。
