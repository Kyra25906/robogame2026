# B3 工作留痕（路线 → 路口转弯 → 坡道 → 取放 → 自主完赛）

> 用途：**任何人（含另一个 Agent、以及明天的你）只看这一份文档，就能知道
> 「做了什么、证据在哪、哪些还没做、下一步是什么」**。
>
> 约定：
> 1. 每轮结束追加一节，不改写历史节（错了就再写一节纠正，保留过程）；
> 2. 每节必须写清：范围 / 改了哪些文件 / 证据（可复现命令 + 实际结果）/
>    能证明什么、不能证明什么 / 下一步；
> 3. 涉及「另一个 Agent 同时改动」时，如实记录文件与结论，不隐去冲突；
> 4. 结论强度只跟证据一致：纯逻辑测试 ≠ 节点行为 ≠ 真车。

---

## R0（B1）路线数据与段推进 ｜ 2026-09-17

**范围**：把比赛计划落成可校验、可执行的 13 段路线数据；不接 ROS。

**产出**
- `ros2_ws/src/robogame_core/robogame_core/mission_route.py`：13 段（`S01…S13`），
  坐标全部按 ID 从实测 `field_layout.yaml` 解析；构造即自检（段间位姿连续、
  转向与几何一致、限速 ≤ 底盘限幅、依赖位姿的判据必须标 `estimated`）。
- `mission.py`：新增 `ROUTE_RUNNING` / `MissionPhase` / 每段独立超时预算；
  旧演示流程保留（`route=None` 时行为不变）。
- `tests/test_mission_route.py`（65 项）。

**证据**：`D:\python.exe -B tools\run_tests.py tests.test_mission_route` → 65 OK。
自检抓出两个真问题：① `field_layout.yaml` 的 yaw 只写 4 位小数，1e-6 容差把实测
精度误判成矛盾（改用 1e-3）；② 转向边界上航向「本来就该差 90°」，原连续性检查
把它当矛盾（改成「边界航向变化必须等于登记的转向方向」）。

**不能证明**：真车能否沿线走、路口转得准不准。

---

## R1（B2）路线接进任务层 + 唯一底盘授权 ｜ 2026-09-17

**范围**：让 13 段真的被驱动；同一时刻只允许一个来源驱动底盘；网页可见。

**产出**
- `mission_manager/node.py`：`route_enabled` 路线模式（喂 `/line_follow/status`
  与 `/pose`、按段发 `/motion/goal`、发机构命令、上报 `/mission/route`）；
  路线不可用 → 判失败停车，**不退回演示流程**。
- `mission_dispatch.py`（新）：段→授权/目标/机构命令的纯决策；`/line_follow/status`
  解析（失败一律 `stale=True`）。
- `route_loader.py`（新）：读哪份场地图 + yaml 读取（PyYAML 优先，缺库用兜底读取器）。
- 授权门控：`motion_control`、`line_follow_controller` 未授权不发非零速度，
  且**只在「自己本来在驱动」时发一次归零**（持续发零速会抢 `/cmd_vel`）。
- `tools/launch_params.py`（新）：按「配置层 + 内联覆盖」算某节点参数的**最终生效值**。

**证据**：全量 1051 → 1093 项，除 2 个已知沙箱错误（`test_field_console` 的
`WinError 5`，与代码无关）全过。

**与另一个 Agent 的真实交互（如实记录）**
1. 会话中途 `HEAD` 被推进多个「保护快照」提交（`ff78fe4` 等），把我进行中的
   `mission_route.py` / `junction_turn.py` 一并提交。
2. 对方把「独立巡线联调图」改成给两个节点传 `common+field`，于是我在**共用层**
   写的 `require_authorization: true` 生效 → 那张图没有任务层 → 巡线节点永远
   等不到授权、一动不动。**这是我的层次放错**（环境属性不该放共用层）：
   改为共用层 `false`（`robot.yaml`）、比赛层 `true`（`robot_field.yaml`）、
   `hardware.launch.py` 给巡线节点补传 `field` 层；对方同步了注释与内联覆盖。
3. 由此教训：文本级断言查不出这类跨文件语义冲突 → 新增
   `effective_parameter()` 按层解析最终值后再断言。

---

## R2（B3-1）路口转弯纯逻辑 + 现场待测清单 ｜ 2026-09-17

**范围**：转弯状态机（纯逻辑）；把所有「猜的值」写成可现场执行的清单。

**产出**
- `robogame_core/junction_turn.py`（新）：`APPROACH → TURNING → SETTLE → DONE/FAILED`，
  主判据是巡线阵列（路口签名 → 线重新居中 + 稳定 N 拍），**不赌里程计**；
  航向确认可选（默认关）；读数过期一律停车、久则失败。
- `mission_dispatch.turn_command_for()` / `parse_turn_command()`：任务层→巡线节点的
  转弯命令契约（JSON，坏输入返回 None）。
- `docs/field/现场待测清单_B2B3_待填值.md`（新）：A 类阻断项 / B 类阈值 /
  B4 转弯参数 / C 类未实现动作 / D 类必留证据 / E 类占位值一览。

**证据**：`tests.test_junction_turn` 39 OK。抓出两个真问题：①「稳定 N 拍」曾
off-by-one 成 N+1；② 航向基线若在转向开始那拍缺失，会永远判不出完成 → 改为
转向途中首次拿到航向时补采（并记录其局限）。

**不能证明**：真车的路口波形、0.6 rad·s⁻¹ 会不会转过头、`min_turn_s` 比例。

---

## R3（B3-2 前置）网页里程计标定：三问分离 ｜ 2026-09-17

**背景**：用户提出「只验证了给速度、给时间、距离基本一致」，需要网页里再测。
复核发现现有「底盘定距测试」是**用里程计闭环停车**，所以「已前进 = 目标」是自证，
**证明不了里程计准不准**。

**产出**
- `tools/odom_calibration.py`（新，纯逻辑）：把三个问题分开算并各自下结论——
  ① 开环（命令速度×时间 vs 尺量）；② **自洽**（里程计等效速度 ÷ 命令速度，
  **不用尺子**就能看出被放大几倍）；③ 标度（尺量 ÷ 里程计）。样本 <3 组或
  组间波动 >3% 时**拒绝给标度**；接近整数比时提示先查固件常量。
- `tools/field_dashboard.py`：`/api/odom/record`（尺量**可留空**，用于累积自洽
  样本）、`/api/odom/reset`、snapshot 增加 `odom_calibration` 与候选；中断的
  定距试验清掉候选（防止把旧数据当本次数据）。
- `tools/field_dashboard_web/odom_panel.js` + `index.html` + `app.js`：新面板。

**证据**：`tests.test_odom_calibration` 22 OK、`tests.test_dashboard_odom` 13 OK
（**行为级**：真实 `DashboardController` + Mock ROS，真跑「定距→补录尺量→汇总」）、
`node tmp\odom_panel_check.js` 5/5；全量 1128 项，仅 2 个已知沙箱错误。

**设计修正（留痕）**：初版要求「必须填尺量才能记录」，把最有用的②锁住了 → 改为
尺量可留空。

**不能证明**：真车里程计准不准——只有场地跑 3 组才知道。

---

## R4（B3-2）转弯器接进节点 + 网页显示转弯阶段 ｜ 2026-09-17

**范围**：让 R2 的转弯纯逻辑真的被执行；转弯阶段在网页可见。

**产出**
- `mission_manager/node.py`：新增发布 `/mission/turn`；进入段时下发一次
  （`turn_command_for(segment)`），非转弯段下发**空串**（清除）；
  `_release_authority()`（终态/失败）也会清空——**防止任务失败后残留的转弯指令
  在下次授权时突然执行**。
- `line_follow_node.py`：订阅 `/mission/turn` → `parse_turn_command` → `JunctionTurner`；
  `_tick` 里转弯命令**接管输出**（PD 让位）；只在**本节点被授权**时推进转弯计时
  （未授权则停住、不消耗超时预算）；读数过期照旧停车；状态串新增
  `turn_phase / turn_reason / turn_pending / turn_last_result`（结构化 JSON，供网页）。
- **安全取舍（重要）**：转弯 `FAILED` 后**保持零速、不回退 PD**——方向没转对时
  继续往前开是危险的；等任务层重发（重试）或判失败。这条有专门的 AST 断言钉住。
- `tools/field_dashboard_web/route_panel.js`：实时行显示「转弯阶段 X」（从
  `telemetry.line_diag.turn_phase` 读）。

**证据**
- `tests.test_mission_manager_route` 28 → 35 OK：新增 7 项接线断言（发布/清除、
  订阅/解析、接管输出、只在授权时推进、失败不回退 PD、状态字段齐全）+
  1 项「转弯命令只在本节点被授权时才到达 `/cmd_vel`」的仲裁组合测试。
- `tests.test_dashboard_route` 13 OK（新增 1 项：面板读 `turn_phase` 且节点确实上报）。
- `node tmp\route_panel_check.js` 13/13。
- 全量 **1136** 项，仅 2 个已知沙箱错误。

**登记式测试更新（说明原因，非放宽）**：巡线节点新增订阅 `/mission/turn` 后，
两张「独立巡线图」多出一个已解释缺口（那两张图刻意没有任务层）——
`tests/test_launch_graph.py` 与 `tests/test_line_follow_nodes_ast.py` 的登记同步为
`{/mission/active_source, /mission/turn}`，豁免的语义前提（该图不要求授权）仍由
`test_standalone_line_graphs_do_not_require_authorization` 按层解析强制。

**不能证明**：转弯在真车上能否成功（波形、角速度、最短转向时间全部未验证）；
节点行为在本机跑不了（无 rclpy），接线是 AST 结构断言 + 纯逻辑组合测试。

**下一步**：坡道段（C3 限速/打滑接入）→ W02 连取 3 块与 W04 搭 2 层动作序列 →
B4 整栈自主启动 / 故障降级。

---

## R5（B3-3）每段巡线限速 + 坡道段（C3 接入） ｜ 2026-09-17

**范围**：把 C3 的 `RampController` 接进路线执行；顺带补一个此前没被发现的缺口。

**发现（真实缺口）**：路线登记表里每段都写了限速（0.20 / 0.25 / 0.15），但巡线节点
一直用自己参数里的 `vx_base`——**等于全路段一个速度**，计划里的分段限速从未生效。
本轮由任务层按段下发。

**产出**
- `mission_dispatch.py`：新增 `LineCommand` + `line_command_for()` / `parse_line_command()`
  / `effective_line_speed()`。巡线段下发本段限速；坡道段额外带 `RampProfile`（C3）。
- `mission_manager`：新增发布 `/mission/line`（进入段下发一次，非巡线段发空串清除，
  终态也清除）；新增**重试时重发本段命令**——打滑卡住后巡线节点保持零速，
  只重置计时是叫不醒它的；`/line_follow/status` 里出现 `STUCK` 连续 3 次 →
  走既有的 `action_failed` 重试路径（`MECHANISM_ERROR: ramp stuck`）。
- `line_follow_node`：订阅 `/mission/line` 与 `/wheel_odom`；`_apply_line_limits()`
  按本段限速裁剪 PD 输出，坡道段把输出穿过 `RampController`（限速/加速度/打滑降速）；
  轮速**过期时不做打滑判定**（宁可不判，也不误判停车）；状态串新增
  `line_limit_mps / ramp_kind / ramp_decision / measured_speed_mps`。
- `/mission/route` 载荷与网页面板：显示「本段限速」「坡道正常/SLIPPING/STUCK」。

**证据**
- `tests.test_mission_dispatch` 30 → 40 OK（新增 10 项：分段限速、只有坡道段带
  profile、JSON 往返、坏输入拒收、取更严格的限速、坡道限速/下坡更保守/轮速跟不上
  → SLIPPING → STUCK 停车、正常轮速不误报）。
- `tests.test_mission_manager_route` 35 → 43 OK（新增 8 项接线断言，含「重试必须
  重发命令」「STUCK 触发重试」「轮速过期禁用打滑判定」）。
- `node tmp\route_panel_check.js` 14/14；全量 **1154** 项，仅 2 个已知沙箱错误。

**登记式测试更新**：巡线节点新增订阅 `/mission/line` → 两张独立巡线图的已解释缺口
变为 `{active_source, turn, line}`（原因同 R4）。

**必须说清的边界（已写进代码注释与现场清单）**
1. 打滑检测用**轮速**里程计：它看不到「轮子空转、车不走」（编码器量的是轮子转了
   多少）。它能抓的是「轮速跟不上命令」（上坡上不去的典型表现）。真抓空转需要
   IMU 俯仰/加速度或视觉，而 `imu_valid` 目前为 false。
2. `vx_base` 同时是**全局兜底上限**：生效限速取 `min(本段限速, vx_base)`。
   要让 S02 的 0.25 真正生效，节点参数 `vx_base` 必须 ≥ 0.25。

**不能证明**：坡道限速/打滑阈值是否合适（14° 实坡未测）；节点行为仍是 AST 断言。

**下一步**：W02 连取 3 块（槽间微移）+ W04 搭 2 层动作序列 → B4 整栈自主启动。

---

## R6（B3-4）作业段动作序列：W02 连取 3 块 + W04 搭 2 层 ｜ 2026-09-17

**范围**：把「连发 3 次 `PICK_ORANGE`」升级为**逐步动作序列**（抓放 + 之间的车体微移）。

**为什么必须做**：机构只管抓，**方块的横向位置是车给的**。墙材区 10 个槽、槽距约
150 mm，抓完一块必须让车侧移一个槽距才能抓下一块——只重复发命令会反复抓同一个
位置（抓空或把已抓的撞掉）。搭建同理：底 2 顶 1 需要同层侧移一个方块宽、换层退回中间。

**产出**
- `robogame_core/work_sequence.py`（新，纯逻辑）：`WorkStep`（抓/放/微移）、
  `work_steps()`（由**载货计划 + 摆法**推导序列）、`shifted_pose()`（车体系→世界系
  位移换算，正 lateral = 车的右侧）、`WorkPlan`（步骤推进器）。
  - 抓 3 块 → 抓 → 侧移 0.15 → 抓 → 侧移 0.15 → 抓（5 步）
  - 摆法 `2+1` → 放 → 侧移 +0.11 → 放 → 侧移 −0.11 → 放（5 步）
  - 摆法 `1+1` → 放 → 放（2 步，无侧移）——**序列随计划自动变，不是写死的**
- `mission_manager`：作业段进入/重试时 `_prepare_work_plan()` 建序列；
  `_dispatch_work_step()` 逐步下发（抓放→机构命令，侧移→累积位姿目标）；
  **按步切换授权**（抓放归 `manipulator_client`，侧移归 `motion_control`）；
  结果按来源分流（`/motion/result` 与 `/manipulator/result` 分开处理）。
- `/mission/route` 与网页：显示「第 3/5 步：抓第 2 块」。
- `mission_route.py`：新增**不变量**——每个作业段的 `required_count` 必须等于载货
  计划的总块数（防止「抓 3 次但只有 2 块」这种到现场才暴露的错误）。

**证据**
- `tests.test_work_sequence`（新）22 项 OK：序列形状、摆法切换、车体系符号
  （朝 +y 时右侧 = +x）、步进来源必须匹配、`required_count` 与载货一致性负例。
- `tests.test_mission_manager_route` 43 → 48 OK（新增 5 项接线断言，含「侧移成功
  不得计入作业次数」）。
- `tests.test_mission_route` 66 OK（含新不变量负例）。
- `node tmp\route_panel_check.js` 15/15；全量 **1182** 项，仅 2 个已知沙箱错误。

**自己踩的坑（留痕）**：一次编辑把 `route_loader` 的导入删掉了，AST 语法检查通过、
但 `test_node_delegates_loading_to_the_core_loader` 立刻报红——**那条断言救了一次**。

**不能证明（必须现场）**
1. **侧移方向与槽距**：约定「正 lateral = 车的右侧」+ 槽距 0.15 m / 搭建间距 0.11 m
   全是占位值。现场要核对第 1 槽在左还是右、实际槽距、以及麦轮侧移的重复精度。
2. 抓放归机构、侧移归位姿控制——两者交替时**底盘重复定位精度**决定第 2、3 块能否
   对准（已知甲2：机构重复精度受限、只能重试）。
3. 节点行为仍是 AST 断言 + 纯逻辑测试；真车零验证。

**下一步**：B4 整栈自主启动 launch + 无人干预上电流程 + 故障降级。

---

## R7（B4-1）标定持久化 + 上电自主流程 ｜ 2026-09-17

**范围**：拆掉「上电后无人干预」的第一阻断项，并把流程写成可执行文档。

**为什么这是第一阻断项**：网页面板的「应用标定」原本只通过参数服务推给**运行中**
的节点——断电重启就没了。于是每次上电，巡线节点都回到代码默认基准
（`white_ref=0 / black_ref=4095`，只有方向意义、与真实读数无关），归一化结果失效，
巡线直接走不起来。**这不是「精度差一点」，而是「上电自主根本不可能」。**

**产出**
- `robogame_core/line_calibration.py`（新，纯逻辑）：`LineCalibration`（逐路
  white/black + 来源 + 时间戳）、校验（8 路、有限值、`black>white`、分离度 ≥1）、
  `save()`（临时文件 + 替换，掉电不留坏文件）、`load_calibration()`、
  参数↔文件格式互转、`startup_verdict()`（上电结论，**必须能指导操作**）。
- `line_follow_node`：新增 `calibration_file` 参数；启动时加载落盘标定；
  未配置 / 文件缺失 / 校验不过 **一律大声报**（warn/error）并保持默认基准；
  状态串新增 `calibration_file` 与 `calibration_status`（网页看到节点在用哪份标定）。
- `tools/field_dashboard.py`：`--calibration-file`；`_calibrate_apply` 在推送成功后
  **落盘**，写盘失败时**报错而不是报成功**（「已推送给节点但写盘失败」——这正是
  最危险的「看起来成功」）。
- `robot_field.yaml`：巡线节点 `calibration_file: ~/robogame_line_calibration.json`
  （与面板默认路径一致，有测试绑定）。
- `docs/field/上电自主完赛流程.md`（新）：赛前一次性准备 6 项、上电顺序、
  可选 systemd 自启、网页检查点、无人干预期间的唯一合法人工动作、异常恢复、
  **尚未验证清单**。

**证据**
- `tests.test_line_calibration`（新）17 项 OK：往返一致、版本/时间戳、不留临时文件、
  四类非法标定各自被拒、参数互转、上电结论能指导操作。
- `tests.test_calibration_persistence`（新）13 项 OK（**行为级**：真实控制器 +
  Mock ROS，真跑「采集→应用→检查文件」）：写盘成功 / 写盘失败必须报错 /
  校验不过不写盘 / 推送失败不写盘 / 采集时长不足不写盘 / 配置与面板路径一致。
- 全量 **1209** 项，仅 2 个已知沙箱错误。

**自己踩的坑（留痕）**：第一版把落盘代码写进了 `RosFacade`（那里没有
`calibration_file`）→ 运行时必然 `AttributeError`。按职责拆开：facade 只管 ROS、
控制器管文件策略。**接线测试正是为此而写。**

**不能证明**：真车标定能否一次采准、麦轮底盘上标定值是否稳定（同一地面重复标定
的差异未测）；上电自主流程的每一步都还没有真跑过。

**下一步**：故障降级阶梯（段失败重试耗尽后的行为）→ 整栈上电联调（真车）。

---

## R8（B4-2）失败降级阶梯 ｜ 2026-09-17

**范围**：重试耗尽之后怎么办——「一失败就停」放弃剩余时间，「乱走」更危险。

**设计原则**：按**位置与朝向的可信度**分层，而不是按「尽量多跑」。

| 失败的段 | 我们知道什么 | 动作 |
|---|---|---|
| 巡线 / 坡道 / 路口转向 / 车体微移 | 位置或**朝向不可信** | `SAFE_STOP`：停车 + 释放授权 |
| 取块（PICK），框里已有 ≥1 块 | 位置可信、有存货 | `SKIP_TO_BUILD`：跳过剩余取块直接去搭建（已抓的块仍计分） |
| 取块（PICK），一块都没抓到 | 位置可信、没货 | `RETREAT`：退到撤退位（比堵在材料区好） |
| 放置（PLACE） | 位置可信、放置失败 | `RETREAT`：退开，不挡搭建区（已放的块仍计分） |

**产出**
- `robogame_core/mission_recovery.py`（新，纯逻辑）：`RecoveryAction` /
  `RecoveryDecision` / `recovery_for_failure()`；任何不确定输入（段未知、无路线、
  未定义类型）**一律收敛到 `SAFE_STOP`**。
- `RouteRunner.skip_to()`：真正的跳段（跳转后本段累计量清零，判据从头算）。
- `MissionMachine._retry_or_degrade()`：路线模式专用的失败处理——先重试，重试耗尽
  后按阶梯跳段继续（`degradations` 计数、`detail` 写明原因），`SAFE_STOP` 才判死；
  `MissionConfig.degrade_on_failure` 可关（现场对照排查用）。
- `route_progress()` / `/mission/route` 与网页面板：`degradations` 计数，
  面板显示「⚠️ 已降级 N 次」——**否则现场会以为任务在正常推进**。
- `robot_field.yaml`：`degrade_on_failure: true`。

**证据**
- `tests.test_mission_recovery`（新）18 项 OK：阶梯对每个段类型的决策、目标段必须是
  搭建/撤退段、跳段后计数清零、未知目标拒收、状态机级「取块失败有货 → 去搭建并
  继续 ROUTE_RUNNING」「没货 → 撤退」「巡线失败 → 判死并说明安全停车」
  「关掉策略 → 回到判死」「降级后重试预算重置」「旧演示流程不受影响」。
- `node tmp\route_panel_check.js` 16/16；全量 **1230** 项，仅 2 个已知沙箱错误。

**自己踩的坑（留痕）**：测试里多打了一拍——`max_retries=1` 是**第二次**失败触发降级，
多打的那次把新段又带进了重试，于是「detail 里应有 degraded」「retries 应清零」两条
断言同时红。改成「一直失败直到段变化」的写法，节奏与实现一致。

**不能证明**：真车上「抓不到块」到底是哪种失败（抓空/夹爪打滑/视觉没对准），
以及降级后重新找块/找搭建位的成功率——阶梯只是让失败**收场得体**，
不会让失败变成成功。

**下一步**：多轮循环（6 分钟跑多趟）→ 整栈真车联调。

---

## R9（B4-3）比赛时钟 + 多趟循环机制 ｜ 2026-09-17

**范围**：给「6 分钟自主」装一个**时间边界**，并实现多趟循环的机制（默认不开启）。

**产出**
1. **比赛总时钟**（规则 3.2.1：6 分钟，计时结束后动作无效）
   - `MissionConfig.match_time_limit_s`（默认 360 s，0 = 关闭）；路线开始时记
     `route_started_at`，超时立即 `SAFE_STOP` + `MissionResult.SAFETY_STOP`，
     `detail` 写明「比赛时间到，安全停车」。
   - 关键点：**时钟是绝对的，不随段切换重置**——否则永远到不了 6 分钟（有测试）。
   - `route_progress()` / `/mission/route` / 面板显示「剩余 m:ss」。
2. **多趟循环机制**（`build_route_plan(survey, rounds=N)`）
   - 每多一趟：先插一段 `S14_RETURN_TO_LINE_R{k}`（从撤退位 W05 横移 0.3 m 回主路
     N06，车头保持 +y，所以 `heading="free"`），再把取存环 S03…S13 克隆一份
     （id 加 `_R{k}` 后缀、label 标「第 k 趟」）。
   - 连续性由 `RoutePlan` 自检强制：rounds=2 → 25 段、rounds=3 → 37 段，全部通过。
   - **默认 `rounds: 1`**（`robot_field.yaml` 也写明保持 1），原因见下面的阻断项。
3. **降级阶梯在多趟下的修正**（发现并修掉一个真 bug）
   - 原来 `_build_segment_id` 取「路线里第一个搭建段」——多趟时会跳回**上一趟**的
     搭建段，等于倒回去重搭已完成的建筑。改成「取下标 ≥ 当前段的第一处搭建段」，
     并加了测试钉住「第 2 趟取块失败 → 跳第 2 趟的搭建段，且只能向前跳」。

**多趟目前还不能开（两条硬阻断，留痕）**
1. **第二座建筑放哪里**：路线每趟都回到**同一个 W04**，第二趟会在第一座建筑上继续叠，
   而不是新起一座。要真多趟，必须给出第二个落点（新的停车点 + 放置位）。
2. **机构层计数复位**：`manipulator_client` 用 `placed_layers` 递增选放置高度
   （`select_place_height`），跨趟不会归零——第二趟会取到不存在的层高并被钳到最高值。
   需要机构侧提供「新建筑开始」的复位手段（参数/服务）。
   两条都依赖机械/现场结论，不是纯软件能定的事。

**证据**
- `tests.test_mission_recovery` 18 → 27 OK（新增 5 项时钟 + 4 项多趟降级目标）。
- `tests.test_mission_route` 66 → 71 OK（新增 5 项多趟路线：默认仍是 13 段、
  25/37 段、id 唯一、返回段两端与朝向、跨趟连续性、rounds<1 拒绝）。
- `tests.test_mission_manager_route` 48 → 51 OK（rounds/时钟参数接线 + payload）。
- `node tmp\route_panel_check.js` 17/17；全量 **1245** 项，仅 2 个已知沙箱错误。

**自己踩的坑（留痕）**：两处测试断言写错——① 拿真实时钟跑模拟时间（`route_progress()`
内部用 `time.monotonic()`），于是「剩余时间」断言恒为 0 → 给 `route_progress(now=...)`
加了可注入时钟；② 把「多打一拍」的旧节奏又用了一次。都是测试侧问题，实现是对的。

**下一步**：整栈真车联调（`colcon build` → 只起节点 → 架空虚车 → 落地单段）。

---

## 待办边界（R9 结束时仍未做）

| 项 | 状态 |
|---|---|
| 转弯 / 限速 / 坡道 / 取放序列 | ✅ R4–R6（待真车验证） |
| 标定持久化 + 上电流程文档 | ✅ R7（待真车验证） |
| 失败降级阶梯 | ✅ R8（待真车校准） |
| 比赛总时钟（6 分钟边界） | ✅ R9 |
| 多趟循环机制 | ✅ R9（**机制可用但默认关闭**：第二座落点 + 机构层复位两条阻断未解） |
| 真车验证（任何一项） | **零** |

**现场阻断项（来自清单 A 类）**：巡线黑白基准未持久化（重启即丢）；里程计未标定
（网页面板已就绪）；停车点朝向 W01–W08 整段为推断值；放置高度 `[0.10,0.20,0.30]`
与「2 层」计划不符（会搭成 3 层）。
